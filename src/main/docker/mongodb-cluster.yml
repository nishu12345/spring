version: '2'
services:
  stripe-mongodb:
    image: mongo:4.2.7
    ports:
      - '27017:27017'
    command: mongos --configdb csvr/stripe-mongodb-config --bind_ip 0.0.0.0
  stripe-mongodb-node:
    build:
      context: .
      dockerfile: mongodb/MongoDB.Dockerfile
    command: mongod --shardsvr --replSet rs1
  stripe-mongodb-config:
    image: mongo:4.2.7
    container_name: stripe-mongodb-config
    command: mongod --configsvr --dbpath /data/db --replSet csvr
